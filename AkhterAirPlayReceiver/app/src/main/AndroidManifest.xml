<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.READ_PRIVILEGED_PHONE_STATE"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />


    <application
        android:name="com.cjx.airplayjavademo.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/apple"
        android:label="Akhter AppleCast"
        android:largeHeap="true"
        android:roundIcon="@drawable/airplayakhter"
        android:supportsRtl="true"
        android:theme="@style/Theme.AirplayJavaDemo">
        <activity android:name="com.cjx.airplayjavademo.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>